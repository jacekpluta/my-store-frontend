import styled from "styled-components";

const GalleryText = styled.div`
  color: white;
  position: absolute;
  font-weight: bold;
  font-size: 32px;
  z-index: 2;
  top: 40%;
  left: 40px;
  margin: 15px;
`;

export default GalleryText;
