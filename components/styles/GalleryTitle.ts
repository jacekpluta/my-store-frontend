import styled from "styled-components";

const GalleryTitle = styled.div`
  z-index: 5;
  position: absolute;

  font-weight: bold;
  font-size: 48px;
  z-index: 2;
  top: 30%;
  left: 50px;
  margin: 15px;
`;

export default GalleryTitle;
